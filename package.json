{
  "name": "ui-tools-extension",
  "version": "0.13.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "release": "node build.mjs --prod",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
